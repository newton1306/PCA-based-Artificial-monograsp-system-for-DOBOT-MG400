# 📡 Communication Protocol

## 📌 Overview

โปรโตคอลการสื่อสารระหว่าง PC กับ Robot และ ESP32

---

## 🔌 Connection Summary

```
┌────────────────────────────────────────────────────────────────┐
│                      COMMUNICATION MAP                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│   ┌──────────┐         TCP/IP            ┌──────────────┐     │
│   │          │ ───────────────────────── │    DOBOT     │     │
│   │          │    192.168.1.6:29999      │    MG400     │     │
│   │          │                           └──────────────┘     │
│   │    PC    │                                                │
│   │ (Python) │         Serial            ┌──────────────┐     │
│   │          │ ───────────────────────── │    ESP32     │     │
│   │          │    COM9 @ 115200          │ (Gripper+    │     │
│   │          │                           │  LIDAR)      │     │
│   └──────────┘                           └──────────────┘     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🤖 Dobot MG400 TCP/IP Protocol

### Connection Setup
```python
class DobotControllerTCP:
    def connect(self, ip):
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.sock.settimeout(5)
        self.sock.connect((ip, 29999))  # Dashboard port
```

### Initialization Sequence
```python
# 1. Clear any previous errors
self.send_command("ClearError()")
time.sleep(0.5)

# 2. Enable robot motors
self.send_command("EnableRobot()")
time.sleep(4)  # Wait for enable

# 3. Set coordinate frames
self.send_command("User(1)")   # User frame 1
self.send_command("Tool(1)")   # Tool frame 1

# 4. Set speed
self.send_command("SpeedFactor(50)")  # 50% speed
```

### Command Format
```
┌─────────────────────────────────────────────────────────────────┐
│ DOBOT COMMAND FORMAT                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Format: "CommandName(param1, param2, ...)\n"                   │
│                                                                 │
│  Examples:                                                      │
│  ─────────                                                      │
│  "MovJ(100.0, 50.0, -20.0, 45.0)\n"                            │
│     └── X     Y      Z      R (Cartesian)                      │
│                                                                 │
│  "JointMovJ(10.0, 20.0, 30.0, 40.0)\n"                         │
│     └── J1    J2    J3    J4 (Joint angles)                    │
│                                                                 │
│  "SpeedFactor(50)\n"                                            │
│     └── 0-100 percentage                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Available Commands

| Command | Parameters | Description |
|---------|------------|-------------|
| `ClearError()` | - | Clear error state |
| `EnableRobot()` | - | Enable motors |
| `User(n)` | n: 0-9 | Select user frame |
| `Tool(n)` | n: 0-9 | Select tool frame |
| `SpeedFactor(n)` | n: 1-100 | Set speed percentage |
| `MovJ(x,y,z,r)` | mm, deg | Joint interpolated move |
| `MovL(x,y,z,r)` | mm, deg | Linear interpolated move |
| `JointMovJ(j1,j2,j3,j4)` | degrees | Direct joint move |

### Response Format
```python
def send_command(self, cmd):
    self.sock.send((cmd + "\n").encode("utf-8"))
    response = self.sock.recv(1024).decode("utf-8")
    return response

# Response format: "ErrorID,{},CommandName();"
# ErrorID = 0 means success
```

### Example Response
```
Send: "MovJ(100,50,-20,45)\n"
Recv: "0,{},MovJ();"
      └── Success (ErrorID=0)
```

---

## 🦾 ESP32 Serial Protocol

### Connection Setup
```python
class SmartGripperController:
    def connect(self):
        self.serial = serial.Serial(
            port=self.port,       # 'COM9'
            baudrate=115200,
            timeout=2
        )
        time.sleep(2)  # Wait for ESP32 reset
        self.serial.reset_input_buffer()
```

### Command Format
```
┌─────────────────────────────────────────────────────────────────┐
│ ESP32 COMMAND FORMAT                                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Gripper Command:                                               │
│  ─────────────────                                              │
│  Format: "G{angle}\n"                                           │
│  Example: "G45\n"  → Set servo to 45 degrees                   │
│           "G90\n"  → Set servo to 90 degrees                   │
│                                                                 │
│  LIDAR Command:                                                 │
│  ─────────────────                                              │
│  Format: "L\n"                                                  │
│  Response: "LIDAR:{distance}\n"                                │
│  Example: Send "L\n" → Receive "LIDAR:185\n"                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Gripper Control
```python
def send_command(self, cmd):
    self.serial.reset_input_buffer()
    self.serial.write((cmd + '\n').encode())
    time.sleep(0.3)  # Wait for servo movement

# Examples:
gripper.send_command('G22')   # Max open (54mm)
gripper.send_command('G50')   # Mid open (40mm)
gripper.send_command('G96')   # Fully closed (0mm)
```

### LIDAR Reading
```python
def read_lidar(self, samples=5):
    readings = []
    
    for _ in range(samples):
        self.serial.reset_input_buffer()
        self.serial.write(b'L\n')
        
        start = time.time()
        while time.time() - start < 1.0:
            if self.serial.in_waiting > 0:
                response = self.serial.readline().decode().strip()
                
                if response.startswith("LIDAR:") and "ERR" not in response:
                    distance = int(response.split(":")[1])
                    readings.append(distance)
                    break
        
        time.sleep(0.05)
    
    return int(np.median(readings)) if readings else None
```

### LIDAR Response Parsing
```
┌─────────────────────────────────────────────────────────────────┐
│ LIDAR RESPONSE FORMAT                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Success:                                                       │
│  ─────────                                                      │
│  "LIDAR:185\n"                                                 │
│         └── Distance in mm                                      │
│                                                                 │
│  Error:                                                         │
│  ─────────                                                      │
│  "LIDAR:ERR\n"  → Timeout or out of range                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Communication Timing

### Dobot Commands
```
┌──────────────────────────────────────────────────────────────┐
│ DOBOT TIMING                                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  EnableRobot()  │████████████████████│  4000ms needed        │
│                                                              │
│  MovJ(...)      │████│                   ~200ms command      │
│                 │     │████████████│     + movement time     │
│                                                              │
│  Response       │░│                      ~10-50ms            │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### ESP32 Commands
```
┌──────────────────────────────────────────────────────────────┐
│ ESP32 TIMING                                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  G{angle}       │██│                     ~10ms command       │
│                 │  │████│                + 300ms servo       │
│                                                              │
│  L (LIDAR)      │██│                     ~10ms command       │
│                 │  │██│                  + 50-100ms reading  │
│                                                              │
│  100 samples    │████████████████│       ~5000ms total       │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                    COMMUNICATION DATA FLOW                          │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  PYTHON                            ROBOT/ESP32                     │
│  ──────                            ────────────                    │
│                                                                    │
│  ┌─────────────┐                   ┌─────────────┐                │
│  │ Calculate   │                   │   DOBOT     │                │
│  │ coordinates │                   │   MG400     │                │
│  └──────┬──────┘                   └──────▲──────┘                │
│         │                                  │                       │
│         ▼                                  │                       │
│  ┌─────────────┐    TCP/IP         ┌──────┴──────┐                │
│  │ Format      │   "MovJ(...)"     │  Parse &    │                │
│  │ command     │ ────────────────▶ │  Execute    │                │
│  └──────┬──────┘                   └──────┬──────┘                │
│         │                                  │                       │
│         │          "0,{},MovJ();"         │                       │
│         │ ◀──────────────────────────────┘                        │
│         ▼                                                          │
│  ┌─────────────┐                                                  │
│  │ Check       │                                                  │
│  │ response    │                                                  │
│  └─────────────┘                                                  │
│                                                                    │
│  ─────────────────────────────────────────────────────────────    │
│                                                                    │
│  ┌─────────────┐                   ┌─────────────┐                │
│  │ Need        │                   │   ESP32     │                │
│  │ distance    │                   │             │                │
│  └──────┬──────┘                   └──────▲──────┘                │
│         │                                  │                       │
│         ▼                                  │                       │
│  ┌─────────────┐    Serial         ┌──────┴──────┐                │
│  │ Send 'L'    │   "L\n"           │  VL53L0X    │                │
│  │             │ ────────────────▶ │  Read       │                │
│  └──────┬──────┘                   └──────┬──────┘                │
│         │                                  │                       │
│         │         "LIDAR:185\n"           │                       │
│         │ ◀──────────────────────────────┘                        │
│         ▼                                                          │
│  ┌─────────────┐                                                  │
│  │ Parse       │                                                  │
│  │ distance    │                                                  │
│  └─────────────┘                                                  │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## ⚠️ Error Handling

### Dobot Connection Error
```python
try:
    self.sock.connect((ip, 29999))
except socket.timeout:
    print("❌ Connection timeout - check IP and network")
except ConnectionRefusedError:
    print("❌ Connection refused - check if robot is powered")
```

### ESP32 Serial Error
```python
try:
    self.serial = serial.Serial(port, baudrate, timeout=2)
except serial.SerialException:
    print("❌ Cannot open port - check if connected")
```

### LIDAR Timeout
```python
if response is None or "ERR" in response:
    return None  # Caller should handle

# In pick function:
if lidar_dist is None:
    print("❌ LIDAR failed!")
    robot.home()
    return False
```

---

## 🔧 Configuration

```python
# Network Configuration
ROBOT_IP = '192.168.1.6'
DASHBOARD_PORT = 29999

# Serial Configuration
ESP32_PORT = 'COM9'
ESP32_BAUDRATE = 115200

# Timeouts
SOCKET_TIMEOUT = 5      # seconds
SERIAL_TIMEOUT = 2      # seconds
LIDAR_READ_TIMEOUT = 1  # seconds per sample
```
